package com.mk.movieticketbooking.catalog;

/**
 * Physical seat class. Drives per-category pricing (applied later via
 * PricingTier config).
 */
public enum SeatCategory {
  STANDARD,
  PREMIUM,
  RECLINER
}
